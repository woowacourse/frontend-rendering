import { style } from '@vanilla-extract/css';

export const landingHeader = style({
  display: 'flex',
  alignItems: 'center',
  paddingRight: '40px',
  justifyContent: 'space-between',
});

export const landingContents = style({
  width: '90%',

  margin: '0 auto',
});
