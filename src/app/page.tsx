'use client';

import styled from 'styled-components';
import Button from './components/common/Button';

export default function Page() {
  return (
    <div className="space-y-4">
      <Button variant="primary">버튼 생성</Button>
    </div>
  );
}
